spec_version: 1
name: customer_support_env
version: "0.4.0"
description: >
  OpenEnv SupportOps command-center environment: typed actions including internal KB lookup,
  dense step rewards, deterministic multi-signal grader, five tasks (easy / two medium / two hard),
  ops feed + risk/SLA hints, FastAPI + WebSocket server, /health for deployment probes.
type: space
runtime: fastapi
app: server.app:app
port: 7860
