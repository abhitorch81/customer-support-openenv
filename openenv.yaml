spec_version: 1
name: customer_support_env
version: "0.2.0"
description: >
  OpenEnv customer support ticket environment: typed actions, deterministic grader,
  three difficulty-tier tasks (easy / medium / hard), FastAPI + WebSocket server.
type: space
runtime: fastapi
app: server.app:app
port: 7860
