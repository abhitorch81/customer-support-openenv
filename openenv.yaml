spec_version: 1
name: drug_discovery_env
version: "1.0.0"
description: >
  Agentic lead optimization OpenEnv: seed SMILES, RDKit descriptors (MW, LogP, QED, SA, PAINS proxy),
  structured actions (replace/remove/bioisostere/score/submit), five tasks, deterministic grader, /health.
type: space
runtime: fastapi
app: server.app:app
port: 7860
