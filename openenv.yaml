spec_version: 1
name: customer_support_env
version: "0.3.0"
description: >
  OpenEnv customer support operations environment: typed actions, dense step rewards,
  deterministic multi-signal grader, four tasks (easy / medium / medium+ / hard),
  FastAPI + WebSocket server, /health for deployment probes.
type: space
runtime: fastapi
app: server.app:app
port: 7860
